(function (window) {
  window.__env = window.__env || {};
  // API url
    window.__env.BACKEND_URL= "https://localhost:44343/api/",
    // https://entertainment.nbu.edu.sa/
  // Setting this to false will disable console output
  window.__env.enableDebug = true;
})(this);
